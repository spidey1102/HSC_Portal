import SimplifiedPortal from './components/SimplifiedPortal';

/**
 * TreePortal — Classical Hierarchical Examination Directory for NSW HSC.
 * Re-exports the tree directory view as a primary top-level portal component.
 */
export default function TreePortal(props) {
  return <SimplifiedPortal {...props} />;
}
